# MiniBot 运行依赖（精简版）
# Python >= 3.10（依赖 X | None 联合类型语法和 dict[str, Any] 标注）

# ---- 核心 ----
anthropic>=0.40.0       # Claude Messages API SDK

# ---- 配置/解析 ----
PyYAML>=6.0             # SKILL.md frontmatter 解析

# ---- 定时调度 ----
croniter>=2.0           # cron 表达式 → 下次触发时间

# ---- MCP 客户端 ----
# 注意：极简实现自己写 JSON-RPC，不依赖官方 mcp SDK；
# 如果想用官方 SDK，取消下面这行的注释
# mcp>=0.1.0

# ---- 开发/测试 ----
pytest>=8.0
pytest-mock>=3.12       # mock Anthropic client
