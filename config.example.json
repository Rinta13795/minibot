{
  "workspace": "./workspace",
  "model": "claude-sonnet-4-5",
  "max_iterations": 10,

  "identity": "你是 MiniBot，一个谨慎、礼貌、擅长查文档的助手。",

  "tools": {
    "exec": {
      "enabled": true,
      "cmd_whitelist": ["ls", "cat", "grep", "find", "python3", "git"],
      "timeout_sec": 30
    },
    "read_file": {
      "enabled": true,
      "allowed_paths": ["./workspace", "./docs"],
      "max_bytes": 1000000
    },
    "write_file": {
      "enabled": true,
      "allowed_paths": ["./workspace"],
      "forbidden_extensions": [".sh", ".py", ".exe"]
    }
  },

  "skills": {
    "skills_dir": "./skills",
    "disabled": [],
    "_comment_security": "Keep skills_dir OUTSIDE write_file.allowed_paths. A writable skills_dir lets the LLM write a malicious always=true SKILL.md that gets injected into the system prompt. Startup warns on overlap; set allow_writable_skills_dir=true only if you accept the risk."
  },

  "memory": {
    "memory_file": "./workspace/MEMORY.md"
  },

  "mcp_servers": {},

  "scheduled_tasks": []
}
